{
  "name": "subvocal",
  "version": "0.0.2",
  "type": "module",
  "private": true,
  "description": "Tensor-native local coding agent: in-process GGUF inference (CPU + GPU) with AST-aware preprocessing, logit-level steering, speculative decoding, and dual-brain orchestration.",
  "repository": {
    "type": "git",
    "url": "https://github.com/lorenzoperrone/subvocal.git"
  },
  "author": "Lorenzo Luca Perrone",
  "license": "MIT",
  "workspaces": [
    "encode",
    "synapse",
    "utter",
    "utter/packages/*"
  ],
  "bin": {
    "subvocal": "bin/subvocal"
  },
  "scripts": {
    "postinstall": "node scripts/patch-tree-sitter.js",
    "build:native": "node scripts/build-platform.js",
    "build:synapse": "npm run build -w synapse",
    "build:encode": "npm run build -w encode",
    "build": "npm run build:native",
    "test": "npm run test -w encode && npm run test -w synapse",
    "test:fast": "vitest run"
  }
}
